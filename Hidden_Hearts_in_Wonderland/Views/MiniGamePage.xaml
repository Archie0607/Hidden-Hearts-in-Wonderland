<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Hidden_Hearts_in_Wonderland.Views.MiniGamePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Hidden_Hearts_in_Wonderland.Views.Controls"
    BackgroundColor="#E3F2FD"
    BackgroundImageSource="bg2.png">

    <Grid RowDefinitions="Auto,*,Auto"
          Padding="20"
          RowSpacing="16">

        <VerticalStackLayout Spacing="6">
            <Label Text="Memory Match"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#333"
                   HorizontalOptions="Center" />

            <Label Text="{Binding StatusText}"
                   FontSize="14"
                   TextColor="#555"
                   HorizontalOptions="Center" />
        </VerticalStackLayout>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Cards}"
                        SelectionMode="None">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                                 Span="4"
                                 HorizontalItemSpacing="10"
                                 VerticalItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border HeightRequest="92"
                            BackgroundColor="{Binding CardColor}"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="14" />
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectCardCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <Label Text="{Binding DisplayText}"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="#333"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <controls:FantasyButton Grid.Row="2"
                                Text="Restart"
                                Command="{Binding RestartCommand}"
                                ButtonImage="ui_button_blue_normal.png"
                                ButtonWidth="160"
                                ButtonHeight="46"
                                LabelFontSize="14"
                                HorizontalOptions="Center" />
    </Grid>
</ContentPage>
